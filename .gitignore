node_modules/
dist/
build/
coverage/
*.tsbuildinfo

# Segredos: os .env ficam só na máquina. Para o repositório vão apenas os
# `.example` — o repositório é público, e um arquivo de ambiente versionado
# carrega o caminho do banco e o usuário de quem o escreveu.
.env
.env.test
.env.local
.env.production
.env.*.local

.DS_Store
*.log
.vscode/
.idea/

# Ferramental do Claude Code: é ajuste de máquina, não faz parte do projeto.
.claude/

# build do app web
apps/web/dist/

# comprovantes enviados pelos usuários — dado de quem usa, não do projeto
apps/api/arquivos/

# Gravações de tela do app: mostram lançamentos reais de quem aparece nelas.
# O repositório é público — isto nunca deve subir.
Vídeo Explicativo/
*.mov
*.mp4
